<section class="page">
  <div class="wrap narrow">
    <% if (foundingSpots !== null && foundingSpots > 0) { %>
    <div class="panel founding-banner rise" style="text-align:center;margin-bottom:var(--sp-4);padding:var(--sp-4) var(--sp-5);background:linear-gradient(135deg,rgba(212,175,55,.12),rgba(212,175,55,.04));border:1px solid rgba(212,175,55,.35)">
      <div style="font-size:1.5rem;margin-bottom:.25rem">🥂</div>
      <strong class="serif" style="font-size:1.25rem;color:var(--gold)">Founding Member Offer</strong>
      <p class="muted mt-2" style="font-size:.95rem">Join now and get <strong style="color:var(--text)"><%= foundingMonths %> months of Gold free</strong> — no credit card required.</p>
      <p class="mt-1" style="font-size:.85rem;color:var(--rose)"><strong><%= foundingSpots.toLocaleString() %></strong> of <%= (foundingSpots + (500 - foundingSpots)).toLocaleString() %> founding spots remaining</p>
    </div>
    <% } %>

    <div class="card card-pad rise" style="max-width:520px;margin:0 auto">
      <span class="eyebrow">Create account</span>
      <h1 class="mt-2 mb-2">Join <%= brand.name %></h1>
      <p class="muted mb-4">Free to create a profile. It takes about two minutes.</p>

      <% if (error) { %><div class="alert-inline"><%= error %></div><% } %>

      <form action="/signup" method="post">
        <div class="field">
          <label for="account_type">I'm signing up as a…</label>
          <select name="account_type" id="account_type" required>
            <% Object.keys(accountTypes).forEach(function(key){ %>
              <option value="<%= key %>" <%= values.account_type === key ? 'selected' : '' %>><%= accountTypes[key] %></option>
            <% }) %>
          </select>
        </div>
        <div class="field">
          <label for="email">Email</label>
          <input type="email" name="email" id="email" value="<%= values.email || '' %>" required autocomplete="email" />
        </div>
        <div class="field">
          <label for="password">Password</label>
          <input type="password" name="password" id="password" required minlength="8" autocomplete="new-password" />
          <div class="hint">At least 8 characters.</div>
        </div>
        <label class="switch mb-3">
          <input type="checkbox" name="age_confirm" required />
          <span class="track"></span>
          <span>I confirm I am 18 years or older.</span>
        </label>
        <button class="btn btn-primary btn-block" type="submit">Create my profile</button>
      </form>
      <p class="text-sm muted mt-3 center">
        Already a member? <a href="/login" class="text-gold">Sign in</a>
      </p>
    </div>
  </div>
</section>
